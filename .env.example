# Environment Configuration

This project uses environment variables for configuration. Copy `.env.example` to `.env` and update values as needed.

## Setup

```bash
cp .env.example .env
```

## Variables

- `API_BASE_URL` - Base URL for the CRED API
- `API_TIMEOUT` - Request timeout in seconds (default: 30)
